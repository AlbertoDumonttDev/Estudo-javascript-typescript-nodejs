"use strict"

const container =  document.querySelector('.container');

console.log(container.firstChild); // Retorna o primeiro nó filho do elemento #text
console.log(container.firstElementChild); // Retorna o primeiro elemento filho do elemento

const p =  document.querySelector('.paragrafo');

console.log(p.firstChild); // Retorna o primeiro nó filho do elemento #text
console.log(p.firstElementChild);