"use strict"

const container = document.querySelector('.container');

console.log(container.lastElementChild); // Retorna o ultimo nó filho do elemento #text
console.log(container.lastChild); // Retorna o ultimo nó filho do elemento #text